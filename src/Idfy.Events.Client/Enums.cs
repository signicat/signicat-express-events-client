namespace Idfy.Events.Client
{
    public enum FileFormat
    {
        unsigned,
        native,
        standard_packaging,
        pades,
        xades
    }

    public enum LogLevel
    {
        Debug,
        Info,
        Warn,
        Error,
    }
}